<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Version</title>
    <link rel="shortcut icon" href="<%= logoImage %>" type="image/x-icon">
    <link rel="stylesheet" href="/assets/css/styles.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.5/font/bootstrap-icons.min.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-200 text-gray-900 dark:bg-gray-800 dark:text-gray-200 flex">
    <%- include('../../partials/dashboardHeader'); %>

    <!-- Main Content -->
    <main class="flex-1 ml-64 lg:ml-72 p-6 space-y-8 overflow-auto">
      <div class="container mx-auto">
        <h1 class="text-3xl font-semibold mb-6">Edit Version</h1>
        <form action="<%= api.https %>://<%= api.baseURL %><% if (process.env.NODE_ENV === 'development') { %>:<%= api.port %><% } %>/api/versions/update/<%= version._id %>" method="POST" class="bg-white dark:bg-gray-900 shadow-md rounded-lg p-6 space-y-4">
          <div>
            <label for="name" class="block text-gray-700 dark:text-gray-200">Version Name</label>
            <input type="text" name="name" id="name" value="<%= version.name %>" class="mt-1 block w-full border border-gray-300 dark:border-gray-700 rounded-md shadow-sm focus:ring focus:ring-blue-500 focus:border-blue-500" required />
          </div>

          <div>
            <label for="tag" class="block text-gray-700 dark:text-gray-200">Version Tag</label>
            <input type="text" name="tag" id="tag" value="<%= version.tag %>" class="mt-1 block w-full border border-gray-300 dark:border-gray-700 rounded-md shadow-sm focus:ring focus:ring-blue-500 focus:border-blue-500" required />
          </div>

          <div>
            <label for="shortDescription" class="block text-gray-700 dark:text-gray-200">Short Description</label>
            <textarea name="shortDescription" id="shortDescription" class="mt-1 block w-full border border-gray-300 dark:border-gray-700 rounded-md shadow-sm focus:ring focus:ring-blue-500 focus:border-blue-500" required><%= version.shortDescription %></textarea>
          </div>

          <div>
            <label for="detailedDescription" class="block text-gray-700 dark:text-gray-200">Detailed Description</label>
            <textarea name="detailedDescription" id="detailedDescription" class="mt-1 block w-full border border-gray-300 dark:border-gray-700 rounded-md shadow-sm focus:ring focus:ring-blue-500 focus:border-blue-500" required><%= version.detailedDescription %></textarea>
          </div>

          <div>
            <label for="features" class="block text-gray-700 dark:text-gray-200">Features (comma-separated)</label>
            <input type="text" name="features" id="features" value="<%= version.features.join(', ') %>" class="mt-1 block w-full border border-gray-300 dark:border-gray-700 rounded-md shadow-sm focus:ring focus:ring-blue-500 focus:border-blue-500" />
          </div>

          <div>
            <label for="added" class="block text-gray-700 dark:text-gray-200">Added (comma-separated)</label>
            <input type="text" name="added" id="added" value="<%= version.added.join(', ') %>" class="mt-1 block w-full border border-gray-300 dark:border-gray-700 rounded-md shadow-sm focus:ring focus:ring-blue-500 focus:border-blue-500" />
          </div>

          <div>
            <label for="fixed" class="block text-gray-700 dark:text-gray-200">Fixed (comma-separated)</label>
            <input type="text" name="fixed" id="fixed" value="<%= version.fixed.join(', ') %>" class="mt-1 block w-full border border-gray-300 dark:border-gray-700 rounded-md shadow-sm focus:ring focus:ring-blue-500 focus:border-blue-500" />
          </div>

          <div>
            <label for="bugs" class="block text-gray-700 dark:text-gray-200">Bugs (comma-separated)</label>
            <input type="text" name="bugs" id="bugs" value="<%= version.bugs.join(', ') %>" class="mt-1 block w-full border border-gray-300 dark:border-gray-700 rounded-md shadow-sm focus:ring focus:ring-blue-500 focus:border-blue-500" />
          </div>

          <div>
            <label for="releasedAt" class="block text-gray-700 dark:text-gray-200">Released At</label>
            <input type="date" name="releasedAt" id="releasedAt" value="<%= new Date(version.releasedAt).toISOString().split('T')[0] %>" class="mt-1 block w-full border border-gray-300 dark:border-gray-700 rounded-md shadow-sm focus:ring focus:ring-blue-500 focus:border-blue-500" required />
          </div>

          <div>
            <label for="developers" class="block text-gray-700 dark:text-gray-200">Developers (comma-separated)</label>
            <input type="text" name="developers" id="developers" value="<%= version.developers.join(', ') %>" class="mt-1 block w-full border border-gray-300 dark:border-gray-700 rounded-md shadow-sm focus:ring focus:ring-blue-500 focus:border-blue-500" />
          </div>

          <div>
            <label for="downloadLink" class="block text-gray-700 dark:text-gray-200">Download Link</label>
            <input type="url" name="downloadLink" id="downloadLink" value="<%= version.downloadLink %>" class="mt-1 block w-full border border-gray-300 dark:border-gray-700 rounded-md shadow-sm focus:ring focus:ring-blue-500 focus:border-blue-500" required />
          </div>

          <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded shadow-md hover:bg-blue-600 transition duration-300">Update Version</button>
        </form>
      </div>
    </main>
  </body>
</html>

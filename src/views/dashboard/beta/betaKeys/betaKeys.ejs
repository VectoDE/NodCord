<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Beta-Schlüssel Übersicht</title>
  <link rel="shortcut icon" href="<%= logoImage %>" type="image/x-icon">
  <link rel="stylesheet" href="/assets/css/styles.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.5/font/bootstrap-icons.min.css" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-200 text-gray-900 dark:bg-gray-800 dark:text-gray-200 flex">
  <%- include('../../../partials/dashboardHeader'); %>

  <!-- Main Content -->
  <main class="flex-1 ml-64 lg:ml-72 p-6 space-y-8 overflow-auto">
    <div class="container mx-auto">
      <!-- Title and Create Button -->
      <div class="flex justify-between items-center mb-6">
        <h1 class="text-3xl font-semibold">Beta-Schlüssel Übersicht</h1>
        <a href="/dashboard/beta/keys/create" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded shadow-md transition duration-300">
          <i class="bi bi-plus-circle"></i> Neuen Beta-Schlüssel erstellen
        </a>
      </div>

      <!-- Beta Keys Table -->
      <div class="bg-white dark:bg-gray-900 shadow-md rounded-lg overflow-hidden">
        <table class="min-w-full bg-white dark:bg-gray-900">
          <thead>
            <tr class="w-full bg-gray-200 dark:bg-gray-800">
              <th class="text-left py-3 px-4 uppercase font-semibold text-sm">Key</th>
              <th class="text-left py-3 px-4 uppercase font-semibold text-sm">Name</th>
              <th class="text-left py-3 px-4 uppercase font-semibold text-sm">Beschreibung</th>
              <th class="text-left py-3 px-4 uppercase font-semibold text-sm">Benutzer</th>
              <th class="text-left py-3 px-4 uppercase font-semibold text-sm">Aktionen</th>
            </tr>
          </thead>
          <tbody class="text-gray-700 dark:text-gray-200">
            <% if (betaKeys && betaKeys.length > 0) { %>
              <% betaKeys.forEach(key => { %>
                <tr class="border-t border-gray-200 dark:border-gray-700">
                  <td class="py-3 px-4"><%= key.key %></td>
                  <td class="py-3 px-4"><%= key.name %></td>
                  <td class="py-3 px-4"><%= key.description || 'Keine Beschreibung' %></td>
                  <td class="py-3 px-4"><%= key.user ? key.user.username : 'Kein Benutzer' %></td>
                  <td class="py-3 px-4 flex space-x-2">
                    <a href="/dashboard/beta/keys/edit/<%= key._id %>" class="text-yellow-500 hover:text-yellow-600 transition duration-300">
                      <i class="bi bi-pencil-square"></i> Bearbeiten
                    </a>
                    <form action="/api/beta/keys/delete/<%= key._id %>" method="POST" onsubmit="return confirm('Möchten Sie diesen Beta-Schlüssel wirklich löschen?');">
                      <button type="submit" class="text-red-500 hover:text-red-700 transition duration-300">
                        <i class="bi bi-trash"></i> Löschen
                      </button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            <% } else { %>
              <tr>
                <td colspan="5" class="text-center py-3 px-4 text-gray-500 dark:text-gray-400">Keine Beta-Schlüssel gefunden.</td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </main>
</body>
</html>

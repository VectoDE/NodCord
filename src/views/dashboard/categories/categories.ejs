<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Categories</title>
  <link rel="shortcut icon" href="<%= logoImage %>" type="image/x-icon">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.5/font/bootstrap-icons.min.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-200 text-gray-900 dark:bg-gray-800 dark:text-gray-200 flex">
  <%- include('../../partials/dashboardHeader'); %>
  <main class="flex-1 ml-64 lg:ml-72 p-5 space-y-8 overflow-auto">
    <h1 class="text-3xl font-semibold mb-6">Categories</h1>
    <a href="/dashboard/categories/create" class="bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded shadow-md inline-block mb-4">
      <i class="bi bi-plus-circle"></i> Create New Category
    </a>
    <div class="bg-white dark:bg-gray-900 p-6 rounded-lg shadow-md">
      <ul class="space-y-4">
        <% categories.forEach(category => { %>
          <li class="flex justify-between items-center border-b border-gray-200 dark:border-gray-700 pb-2 mb-2">
            <div>
              <strong class="text-lg"><%= category.name %></strong> - <%= category.description %>
            </div>
            <div class="flex space-x-2">
              <a href="/dashboard/categories/update/<%= category._id %>" class="text-blue-500 hover:text-blue-600">Edit</a>
              <form action="/api/categories/delete/<%= category._id %>" method="POST">
                <button type="submit" class="text-red-500 hover:text-red-600">Delete</button>
              </form>
            </div>
          </li>
        <% }) %>
      </ul>
    </div>
  </main>
</body>
</html>

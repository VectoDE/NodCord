<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Analytics Overview</title>
  <link rel="shortcut icon" href="<%= logoImage %>" type="image/x-icon">
  <link rel="stylesheet" href="assets/css/styles.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.5/font/bootstrap-icons.min.css" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-200 text-gray-900 dark:bg-gray-800 dark:text-gray-200 flex">
  <%- include('../../partials/dashboardHeader'); %>
  <main class="flex-1 ml-64 lg:ml-72 p-5 space-y-8 overflow-auto">
    <div class="container mx-auto">
      <h1 class="text-3xl font-semibold mb-6">Analytics Overview</h1>

      <!-- General Analytics Overview -->
      <div class="bg-white dark:bg-gray-900 rounded-lg shadow-md p-6">
        <h2 class="text-xl font-semibold mb-4">General Analytics</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <!-- Example Card 1 -->
          <div class="bg-gray-100 dark:bg-gray-700 rounded-lg p-4">
            <h3 class="text-lg font-medium mb-2">Total Visits</h3>
            <p class="text-2xl font-bold"><%= totalVisits %></p>
          </div>
          <!-- Example Card 2 -->
          <div class="bg-gray-100 dark:bg-gray-700 rounded-lg p-4">
            <h3 class="text-lg font-medium mb-2">New Sign-ups</h3>
            <p class="text-2xl font-bold"><%= newSignUps %></p>
          </div>
          <!-- Example Card 3 -->
          <div class="bg-gray-100 dark:bg-gray-700 rounded-lg p-4">
            <h3 class="text-lg font-medium mb-2">Total Sales</h3>
            <p class="text-2xl font-bold">$<%= totalSales %></p>
          </div>
        </div>
      </div>

      <!-- Detailed Analytics List -->
      <div class="bg-white dark:bg-gray-900 rounded-lg shadow-md p-6 mt-6">
        <h2 class="text-xl font-semibold mb-4">Detailed Analytics</h2>
        <% if (analytics.length > 0) { %>
        <table class="min-w-full bg-white dark:bg-gray-800 rounded-lg shadow-md">
          <thead>
            <tr class="bg-gray-200 dark:bg-gray-700 text-gray-900 dark:text-gray-200">
              <th class="py-3 px-4 border-b">ID</th>
              <th class="py-3 px-4 border-b">Metric</th>
              <th class="py-3 px-4 border-b">Value</th>
              <th class="py-3 px-4 border-b">Date</th>
              <th class="py-3 px-4 border-b">Actions</th>
            </tr>
          </thead>
          <tbody>
            <% analytics.forEach(item => { %>
            <tr class="hover:bg-gray-100 dark:hover:bg-gray-700">
              <td class="py-3 px-4 border-b"><%= item.id %></td>
              <td class="py-3 px-4 border-b"><%= item.metric %></td>
              <td class="py-3 px-4 border-b"><%= item.value %></td>
              <td class="py-3 px-4 border-b"><%= new Date(item.date).toLocaleDateString() %></td>
              <td class="py-3 px-4 border-b">
                <a href="/dashboard/analytics/<%= item.id %>" class="text-blue-500 hover:underline">Details</a>
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
        <% } else { %>
        <p class="text-gray-600 dark:text-gray-400">No analytics data available.</p>
        <% } %>
      </div>
    </div>
  </main>
</body>
</html>

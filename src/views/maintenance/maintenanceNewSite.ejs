<!DOCTYPE html>
<html lang="de">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Neue Website Einführung</title>
  <link rel="shortcut icon" href="<%= logoImage %>" type="image/x-icon">
  <link rel="stylesheet" href="assets/css/styles.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.5/font/bootstrap-icons.min.css" />
  <link rel="stylesheet" href="https://unpkg.com/@merakiui/merakiui/dist/merakiui.min.css" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="dark:bg-gray-900 text-white flex items-center justify-center min-h-screen p-4">
  <div class="bg-gray-800 p-8 rounded-lg shadow-lg max-w-lg w-full text-center">
    <h1 class="text-4xl font-bold text-yellow-400 mb-6 flex items-center justify-center">
      <i class="bi bi-newspaper text-yellow-400 mr-3"></i>
      Neue Website Einführung
    </h1>
    <p class="text-xl mb-6 text-gray-300">
      Wir freuen uns, Ihnen unsere neue Website vorzustellen!
    </p>
    <p class="text-lg mb-6 text-gray-400">
      Das neue Design bietet verbesserte Funktionen und ein besseres Benutzererlebnis.
    </p>
    <p class="text-lg text-gray-400 mb-8">
      Vielen Dank für Ihre Geduld und Unterstützung.
    </p>

    <div class="social-links mb-8">
      <p class="text-lg font-semibold text-gray-300 mb-4">Folge uns auf:</p>
      <div class="flex justify-center gap-6">
        <% if (discord) { %>
          <a href="<%= discord %>" target="_blank" class="text-blue-500 text-2xl hover:text-blue-300">
            <i class="bi bi-discord"></i> Discord
          </a>
        <% } %>
        <% if (twitter) { %>
          <a href="<%= twitter %>" target="_blank" class="text-blue-400 text-2xl hover:text-blue-300">
            <i class="bi bi-twitter"></i> Twitter
          </a>
        <% } %>
        <% if (facebook) { %>
          <a href="<%= facebook %>" target="_blank" class="text-blue-600 text-2xl hover:text-blue-400">
            <i class="bi bi-facebook"></i> Facebook
          </a>
        <% } %>
      </div>
    </div>
  </div>
</body>

</html>

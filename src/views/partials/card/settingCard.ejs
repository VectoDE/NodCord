<div class="bg-white shadow-md rounded-lg max-w-xs sm:max-w-sm md:max-w-md">
  <div class="p-4 text-center">
    <i class="bi bi-gear text-blue-500 text-4xl mb-4"></i>
    <h2 class="text-xl font-semibold mb-2"><%= title || 'Settings' %></h2>
    <p class="text-gray-600 mb-4"><%= description || 'Settings description goes here...' %></p>
    <a href="<%= link || '#' %>" class="inline-flex items-center bg-gray-500 text-white hover:bg-gray-600 rounded px-4 py-2">
      <i class="bi bi-cog mr-2"></i> Adjust Settings
    </a>
  </div>
</div>

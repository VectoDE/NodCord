import { client, startBot, getBots, getMembers, getServers } from '@/bot/bot';

export { client, startBot, getBots, getMembers, getServers };

export default {
  client,
  startBot,
  getBots,
  getMembers,
  getServers,
};

<div class="bg-white shadow-md rounded-lg overflow-hidden max-w-sm">
  <img class="w-full h-48 object-cover" src="<%= image || '/assets/img/default-image.png' %>" alt="<%= title || 'Project Image' %>">
  <div class="p-4">
    <h2 class="text-xl font-semibold mb-2"><%= title || 'Project Title' %></h2>
    <p class="text-gray-600 mb-4"><%= description || 'Project description goes here...' %></p>
    <a href="<%= link || '#' %>" class="inline-flex items-center bg-blue-500 text-white hover:bg-blue-600 rounded px-4 py-2">
      <i class="bi bi-link mr-2"></i> View Project
    </a>
  </div>
</div>

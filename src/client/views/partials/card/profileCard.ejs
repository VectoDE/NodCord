<div class="max-w-xs bg-white shadow-md rounded-lg overflow-hidden">
  <div class="p-4 flex items-center">
    <img class="w-16 h-16 rounded-full mr-4" src="<%= image || '/assets/img/default-profile.png' %>" alt="<%= name || 'Profile Image' %>">
    <div>
      <h3 class="text-lg font-semibold mb-1"><%= name || 'User Name' %></h3>
      <p class="text-gray-600"><%= bio || 'Short bio or description goes here.' %></p>
    </div>
  </div>
</div>
